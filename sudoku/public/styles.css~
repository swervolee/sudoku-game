body {
    font-family: Arial, sans-serif;
    margin: 0;
    background-color: #121212; /* Spotify dark background */
    color: #FFFFFF; /* Text color */
    text-align: center;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

.container {
    background: rgba(255, 255, 255, 0.1); /* Transparent overlay */
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
}

h1 {
    margin-top: 0;
    font-size: 2rem;
    color: #1DB954; /* Spotify green */
}

p {
    color: #B3B3B3; /* Muted text color */
}

#sudoku-board {
    display: grid;
    grid-template-columns: repeat(9, 1fr); /* Create a 9x9 grid */
    gap: 2px; /* Space between cells */
    width: 90%;
    max-width: 450px;
    margin: 20px auto;
    background-color: #535353; /* Board border color */
    border-radius: 10px;
}

#sudoku-board div {
    display: flex;
    justify-content: center;
    align-items: center;
    border: 1px solid #282828; /* Cell border */
    aspect-ratio: 1; /* Ensure cells are square */
    position: relative;
    background-color: #121212; /* Default cell background */
}

#sudoku-board input {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    border: none;
    font-size: 1.5rem;
    text-align: center;
    color: #1DB954; /* Spotify green */
    background-color: transparent; /* Transparent to blend with the cell */
    outline: none; /* Removes the default outline */
    cursor: pointer;
}

#sudoku-board span {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
    font-size: 1.5rem;
    color: #FFFFFF; /* Prefilled number color */
    background-color: #282828; /* Prefilled cell background */
    text-align: center;
    line-height: normal; /* Ensures proper vertical alignment */
}

button {
    background-color: #1DB954; /* Spotify green button */
    color: #FFFFFF;
    font-size: 1.2rem;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    margin-top: 20px;
    transition: background-color 0.3s, transform 0.2s;
}

button:hover {
    background-color: #FFFFFF;
    color: #1DB954; /* Invert button colors */
    transform: scale(1.05);
}

